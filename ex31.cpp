// Concept: Array Declaration
// Reference: Ch 5
// Why: Arrays store multiple elements of the same type in contiguous memory.

#include <iostream>

int main() {
    int arr[5];
    arr[0] = 10;
    std::cout << arr[0] << std::endl;
    return 0;
}

// TODO: Declare an array of 10 doubles and assign 3.14 to the last element.
